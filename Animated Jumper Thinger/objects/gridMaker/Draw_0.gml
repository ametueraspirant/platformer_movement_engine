/// @description testtext

draw_set_halign(fa_center);
draw_set_valign(fa_center);

/*if(Player.debugIs){
	var horiCells = ceil(room_width/gridWidth);
	var vertiCells = ceil(room_height/gridHeight);

	if(ds_exists(global.pathFindingGrid, ds_type_grid)){
		for(var cheese = 0; cheese < horiCells; cheese++){
			for(var curds = 0; curds < vertiCells; curds++){
				draw_text_transformed(cheese * gridWidth + gridWidth/2, curds * gridHeight + gridHeight/2, 
										string(ds_grid_get(global.pathFindingGrid, cheese, curds)), 1, 1, 0);
			}
		}
	}
}*/
